Keyboard key caps for documenting Marginal's shortcuts.

```jsx
<Shortcut keys={['⌘','1']} />
<KeyCap size="sm">⌥</KeyCap>
```

Always use the Mac glyphs (⌘ ⇧ ⌥ ⌃ ↩ ⌫), one cap per key, no "+" separators.
