Wraps a product shot — or a live HTML recreation of the app — in the site screenshot frame.

```jsx
<ScreenshotFrame caption="Marginal · dark mode" theme="dark">
  <EditorWindow />
</ScreenshotFrame>
```

20px radius, hairline border, one large soft shadow. Never tilt, add perspective, or fake browser chrome.
