Site header for marginal.app pages. Sticky, blurred paper, single violet CTA.

```jsx
<NavBar links={[{id:'features',label:'Features'},{id:'docs',label:'Docs'}]}
  cta={{label:'Download', href:'#download'}} active="features" />
```

Never more than five links. The CTA is always the only primary button above the fold.
